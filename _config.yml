safe: true # Required for GitHub Pages
lsi: false # Required for GitHub Pages
highlighter: rouge # Required for GitHub Pages

kramdown:
  input: GFM

defaults:
  -
    scope:
      path: "docs"
      type: "pages"
    values:
      layout: "default"
  -
    scope:
      path: "docs/ModInstaller"
      type: "pages"
    values:
      layout: "default"
      categories: "ModInstaller"
  -
    scope:
      path: "docs/ServerInstaller"
      type: "pages"
    values:
      layout: "default"
      categories: "ServerInstaller"
  -
    scope:
      path: "docs/SiteTester"
      type: "pages"
    values:
      layout: "default"
      categories: "SiteTester"
  -
    scope:
      path: "docs/DatabaseSearcher"
      type: "pages"
    values:
      layout: "default"
      categories: "DatabaseSearcher"
